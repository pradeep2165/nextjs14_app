export default function ForgotPassword() {
  return <h1>Forgot Password</h1>;
}

//now auth foler in route group and its omited its name, hence can be accessed /forgot-password and so for same for all other
